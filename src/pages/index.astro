---
/* no frontmatter needed */
---

<!doctype html>
<html lang="hu">
<head>
  <meta charset="utf-8">
  <title>NA MIVAN HULIGÁNOK</title>

  <meta name="color-scheme" content="light dark">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Major+Mono+Display&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" />

  <style>
    /* 🔧 CONFIG */
    :root {
      --bg: #f4fffb;
      --text: #232d2a;
      --accent: #444;
      --link: #0066cc;
      --glow-strength: 0.15;
      --noise-opacity: 0.04;
    }

    [data-theme="dark"] {
      --bg: #232d2a;
      --text: #f4fffb;
      --accent: #999;
      --link: #7aa2f7;
      --glow-strength: 0.15;
      --noise-opacity: 0.1;
    }

    html, body {
      margin: 0;
      padding: 0;
      background: var(--bg);
      color: var(--text);
      font-family: 'Major Mono Display', monospace;
      text-align: center;
    }

    body {
      min-height: 100vh;
      padding-top: 10vh;
      position: relative;
      overflow-x: hidden;
    }

    a { color: var(--link); text-decoration: none; }
    a:hover { text-decoration: underline; }

    .material-symbols-outlined {
      font-family: 'Material Symbols Outlined';
      color: var(--text);
    }

    body::after {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      box-shadow:
        0 0 40px rgba(255,255,255,var(--glow-strength)),
        inset 0 0 60px rgba(255,255,255,var(--glow-strength));
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      background-image:
        repeating-linear-gradient(
          0deg,
          rgba(255,255,255,var(--noise-opacity)),
          rgba(255,255,255,var(--noise-opacity)) 1px,
          transparent 1px,
          transparent 2px
        );
      mix-blend-mode: overlay;
    }

    #theme-toggle {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: none;
      border: none;
      cursor: pointer;
      opacity: 0.6;
    }

    #theme-toggle:hover { opacity: 1; }
    #theme-toggle span { font-size: 36px; }
  </style>
</head>

<body>
  <span class="material-symbols-outlined"
        style="font-size:120px; opacity:0.75; margin-bottom:10vh;">
    frame_exclamation
  </span>

  <h1>ezt elkúrtad barátom, mehetsz haza</h1>

  <p>meg a nagy túróst hogy ez az oldal ahová akartál menni</p>

  <span class="material-symbols-outlined" style="font-size:48px;">
    sentiment_very_dissatisfied
  </span>

  <p>de ha már itt vagy akkor lentebb van elérhetőség amit úgysem fogok nézni</p>

  <hr style="width:200px; margin:40px auto;">

  <footer style="font-size:0.9em; opacity:0.7;">
    <p>elérhetőség</p>

    <p>💻 github:
      <a href="https://github.com/USERNAME" target="_blank" rel="noopener">
        github.com/USERNAME
      </a>
    </p>

    <p>💬 discord:
      <a href="https://discord.com/users/USERID" target="_blank" rel="noopener">
        USERNAME
      </a>
    </p>
  </footer>

  <button id="theme-toggle" aria-label="toggle theme">
    <span class="material-symbols-outlined">routine</span>
  </button>

  <script>
    const root = document.documentElement;
    const saved = localStorage.getItem("theme");

    function prefersDark() {
      return window.matchMedia("(prefers-color-scheme: dark)").matches;
    }

    function applyTheme(t) {
      t === "dark"
        ? root.setAttribute("data-theme", "dark")
        : root.removeAttribute("data-theme");
    }

    if (saved) applyTheme(saved);
    else if (prefersDark()) applyTheme("dark");

    document.getElementById("theme-toggle").onclick = () => {
      const isDark = root.getAttribute("data-theme") === "dark";
      const next = isDark ? "light" : "dark";
      localStorage.setItem("theme", next);
      applyTheme(next);
    };
  </script>
</body>
</html>

